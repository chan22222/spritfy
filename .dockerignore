# 로컬(Windows) node_modules가 컨테이너의 Linux node_modules를 덮어쓰지 않도록 제외.
# npm ci로 컨테이너 안에서 새로 설치한다.
node_modules
cloudflare-worker/node_modules

# 빌드 산출물(컨테이너에서 새로 생성)
dist

# 기타
.git
.env.local
*.log
npm-debug.log*
